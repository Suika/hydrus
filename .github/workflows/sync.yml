name: Sync Fork
on:
  schedule:
  - cron: '0 23 * * *'
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: TG908/fork-sync@v1.1
      with:
        github_token: ${{ secrets.GITHUB_TOKEN_ACTION }}
        owner: hydrusnetwork
        base: master
        head: master
